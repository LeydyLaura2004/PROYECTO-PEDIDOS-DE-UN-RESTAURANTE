package modelo;

public class Plato extends Producto {
    
    // El constructor DEBE coincidir con el tipo de datos (String, double)
    public Plato(String nombre, double precio) {
        super(nombre, precio);
    }
}